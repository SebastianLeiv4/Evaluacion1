import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'aplicacionDuoc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
